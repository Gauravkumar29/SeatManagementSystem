#spring:
#  datasource:
#    url : jdbc:mysql://localhost:3306/seat_management_system?useSSL=false
#    username : root
#    password : root

spring:
  datasource:
    url: jdbc:mysql://34.70.99.223:3306/seatmanagement?useSSL=false
    username: root
    password: Gaurav@234
    driver-class-name: com.mysql.cj.jdbc.Driver

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


  #jpa Setting
  jpa.hibernate.ddl_auto : update


logging:
  level:
    org.hibernate.type.descriptor.sql: TRACE
    org:
      springframework:
        scheduling: DEBUG